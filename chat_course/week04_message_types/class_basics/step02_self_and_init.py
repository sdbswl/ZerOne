"""
클래스 기초 Step 2 — self 와 __init__ 의 정체
------------------------------------------------------------
클래스에서 학생들이 가장 헤매는 두 가지: self 와 __init__.
비유: 클래스는 '붕어빵 틀', 객체(인스턴스)는 '붕어빵'.
     틀은 하나지만, 붕어빵은 팥·슈크림·피자맛 각각 다르게 구울 수 있다.

Step 1 의 Phone 그대로 시작합니다. 오늘 얹는 것: 배터리.
"""


class Phone:
    # ── Phone 핵심 (Step 1 그대로) ──────────────
    def __init__(self, owner, number):
        # __init__ = 객체가 '태어나는 순간' 자동으로 실행되는 메서드.
        #            (initialize = 초기화)  Phone("철수", ...) 하는 순간 호출된다.
        # self = 지금 태어나고 있는 '바로 그 붕어빵(객체 자신)'.
        self.owner = owner
        self.number = number
        self.power = False
        self.battery = 100            # ★ 오늘 추가: 배터리 (기본값으로 시작)
        print(f"[{self.owner}처음실행")

    def power_on(self):
        self.power = True
        print(f"[{self.owner}의 폰] 전원 ON")

    def call(self, to):
        if not self.power:
            print(f"[{self.owner}의 폰] 전원이 꺼져 있어요!")
            return
        print(f"[{self.owner}의 폰] {to} 에게 전화 📞")

    # ── 오늘의 추가: 배터리 확인·사용 ───────────
    def show_battery(self):
        # 메서드의 첫 번째 자리는 항상 self.
        # "누구의 배터리?" → self(자기 자신)의 배터리.
        print(f"[{self.owner}의 폰] 배터리 {self.battery}%")

    def use(self, minutes):
        self.battery -= minutes          # 자기 배터리를 깎는다
        print(f"[{self.owner}의 폰] {minutes}분 사용")


# ────────────────────────────────────────────
# 1. 붕어빵 굽기: 객체(인스턴스) 만들기
# ────────────────────────────────────────────
print("=== 인스턴스 만들기 ===")
p1 = Phone("철수", "010-1111-1111")     # 이 순간 __init__ 이 실행된다
p2 = Phone("영희", "010-2222-2222")     # 또 하나. 완전히 별개의 붕어빵.


print(p1.owner, p1.number)
print(p2.owner, p2.number)


# # ────────────────────────────────────────────
# # 2. 인스턴스는 서로 '독립'이다
# # ────────────────────────────────────────────
# print("\n=== 인스턴스는 독립 ===")
p1.use(30)             # 철수 폰만 배터리가 닳는다
p1.show_battery()      # 철수: 70%
p2.show_battery()      # 영희: 100% (그대로!)


# # ───────────────────────────────────────────
# # 2b. (참고) 모두가 '공유'하는 값 — 클래스 변수
# # ────────────────────────────────────────────
# # self.battery 처럼 붕어빵마다 다른 값은 '인스턴스 변수'.
# # 반대로, 틀에 새겨져 모든 붕어빵에 똑같이 찍히는 값은 '클래스 변수'입니다.

# class KoreaPhone:
#     country = "대한민국"          # 클래스 변수: def 밖에, self 없이 → 모두 공유

#     def __init__(self, owner):
#         self.owner = owner        # 인스턴스 변수: 객체마다 각자


# print("\n=== 클래스 변수: 모두가 공유 ===")
# k1 = KoreaPhone("철수")
# k2 = KoreaPhone("영희")
# print(k1.owner, k1.country)       # 철수 대한민국
# print(k2.owner, k2.country)       # 영희 대한민국 (같은 값을 본다!)
 
# # week04 의 메시지 클래스들이 갖는  tag = "TEXT",  kind = "텍스트"  가
# # 바로 이 클래스 변수입니다. '그 종류 전체의 이름표'라서 객체마다 다를
# # 필요가 없기 때문에 클래스에 한 번만 새겨 둔 것입니다.

 
# # ────────────────────────────────────────────
# # 3. self 의 비밀: 사실 파이썬이 대신 넣어주는 것
# # ────────────────────────────────────────────
print("\n=== self 의 비밀 ===")
p1.show_battery()          # 우리가 늘 쓰는 방식
Phone.show_battery(p1)     # 사실 위 한 줄은 이것과 완전히 같다!

# # p1.show_battery() 라고 쓰면,
# # 파이썬이 자동으로 p1 을 self 자리에 넣어서 Phone.show_battery(p1) 을 실행한다.
# # → self 는 "점(.) 앞에 있던 그 객체" 다. 그래서 메서드 안에서
# #   self.battery 라고 쓰면 '그 객체의' 배터리를 뜻하게 된다.


# # ────────────────────────────────────────────
# # 4. 소멸자 __del__ : 객체가 '사라질 때' 자동 실행
# # ────────────────────────────────────────────
# # __init__ 이 태어날 때(생성자)라면, __del__ 은 사라질 때(소멸자)입니다.

class TempPhone:
    def __init__(self, owner):
        self.owner = owner
        print(f"[{self.owner}의 폰] 개통되었습니다  (__init__ 실행)")

    def __del__(self):
        print(f"[{self.owner}의 폰] 해지되었습니다  (__del__ 실행)")


print("\n=== 생성자와 소멸자 ===")
t = TempPhone("민수")     # 태어나는 순간 → __init__
del t                     # 지우는 순간   → __del__

# # del 을 직접 쓰지 않아도, 그 객체를 아무도 안 쓰게 되거나
# # 프로그램이 끝나면 파이썬이 정리하면서 __del__ 을 불러 줍니다.
# # 실무에서 파일 닫기 같은 '뒷정리'는 주로 with 문을 씁니다(2주차에 배운 그것!).
# # 지금은 쌍으로 기억하세요: 태어날 때 __init__, 사라질 때 __del__.


# # ────────────────────────────────────────────
# # 5. 자주 하는 실수 모음 (직접 주석을 풀어 실행해 보세요)
# # ────────────────────────────────────────────
# # 실수 1: 메서드에 self 를 안 쓴 경우
# # class Bad1:
# #     def hello():                 # self 가 없다!
# #         print("안녕")
# # Bad1().hello()                   # TypeError: takes 0 arguments but 1 given
# #                                  # → 파이썬이 객체를 넣어주는데 받을 자리가 없어서 에러

# # 실수 2: self. 을 빼먹은 경우
# # class Bad2:
# #     def __init__(self, owner):
# #         owner = owner            # self.owner 가 아니라서 저장이 안 됨!
# # print(Bad2("철수").owner)        # AttributeError

# # 실수 3: __init__ 철자 (밑줄 두 개씩! _init_ 이 아님)
# #         밑줄이 하나면 그냥 이름이 이상한 메서드가 될 뿐, 자동 실행되지 않는다.

# # ┌────────────────────────────────────────────────┐
# # │ 이 프로그램 한 줄 요약:                              │
# # │ self = 점(.) 앞의 그 객체 자신.                 │
# # │ __init__ = 태어날 때, __del__ = 사라질 때       │
# # │ 자동 실행된다.                                  │
# # └────────────────────────────────────────────────┘

# # [직접 해보기]
# # 1. Phone 객체를 3개 만들어 리스트에 담고, for 문으로 전부 show_battery() 해보세요.
# #    (힌트: for p in phones: p.show_battery())  ← 다음 스텝의 복선입니다!
# # 2. charge(양) 메서드를 추가해 배터리를 충전해 보세요. 100을 넘지 않게 하려면?
# # 3. TempPhone 을 두 개 만들어 하나만 del 해 보세요. 남은 하나의 __del__ 은
# #    언제 실행되나요? (힌트: 프로그램이 끝나는 순간을 잘 보세요)
